Place your licensed font files here if you have them:
- Kiona.woff2 (and optional .woff)
- Larkin.woff2 (and optional .woff)
The app will automatically use them if present; otherwise it falls back to Montserrat (headers) and Inter (body).
